# PRD 写作规范

## 只写 What，不写 How

PRD 应描述用户要什么、为什么要、做到什么程度；不描述技术实现。

禁止出现：

- Redis / MQ / 数据库表 / 缓存方案
- 接口字段、内部字段名
- 代码块
- “接口返回 200”这类技术验收口径

## 推荐结构

1. 背景
2. 用户问题
3. 目标用户
4. 用户故事
5. 需求范围
6. 非目标
7. 验收标准
8. 风险与待确认问题

## 验收标准示例

推荐：

- 用户执行命令后，3 秒内能看到清晰的成功或失败提示。
- 当 token 缺失时，用户能看到缺少哪个配置项以及下一步应如何处理。

不推荐：

- 接口返回 HTTP 200。
- 数据库写入成功。
- Redis 缓存命中。
